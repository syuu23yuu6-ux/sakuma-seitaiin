# 🌸 Re'ally nail school 本番公開・独自ドメイン ＆ メール設定マニュアル

本プロジェクトをクライアントへ納品・本番運用を開始するにあたり、必要となる**「独自ドメインの割り当て方法」**および**「お問い合わせ通知用メールアドレスの設定・変更方法」**を、専門知識がなくてもスムーズに行えるようステップバイステップで解説します。

---

## 1. 独自ドメイン（カスタムドメイン）の設定手順

スクールの独自URL（例: `really-nailschool.com`）でサイトを表示するための設定です。

### 【準備】 ドメインの購入
お名前.com、ムームードメイン、または Cloudflare DNS などの外部サービスで、希望するドメイン（例: `really-nailschool.com`）をあらかじめ購入しておきます。

---

### 【ステップ 1】 Cloudflare Pages にドメインを登録する
1. [Cloudflare ダッシュボード](https://dash.cloudflare.com/) にログインします。
2. 左側メニューから **「Workers & Pages」** を選択し、本プロジェクト（`really-nailschool`）を開きます。
3. 上部のタブから **「カスタムドメイン (Custom domains)」** をクリックします。
4. **「カスタムドメインを設定 (Set up a custom domain)」** ボタンをクリックします。
5. 購入した独自ドメイン（例: `really-nailschool.com`）を入力し、**「続行」** をクリックします。

---

### 【ステップ 2】 DNSレコード（CNAME）の書き換え
ドメインを購入したサービス（お名前.comなど）の管理画面にログインし、DNSの設定（レコード追加）を行います。

#### 設定するレコード情報
以下のように CNAME レコードを追加または書き換えます。

| レコードタイプ | ホスト名（サブドメイン） | 値（宛先 URL） | TTL |
| :--- | :--- | :--- | :--- |
| **CNAME** | `www` (または `@`) | `really-nailschool.pages.dev` | 自動 (または 3600) |

> **💡 ホスト名について**
> * URLを `www.really-nailschool.com` にしたい場合は、ホスト名に `www` と入力します。
> * URLを `really-nailschool.com`（wwwなし）にしたい場合は、ホスト名に `@` または空白のまま登録します（お名前.comなどのドメイン会社によって指定方法が異なります）。

---

### 【ステップ 3】 反映と SSL（暗号化）の確認
1. DNSレコードの切り替え後、設定がインターネット全体に反映されるまで **数分〜最大24時間** かかります。
2. 反映されると、Cloudflare Pages の管理画面上でステータスが **「アクティブ (Active)」** に変わります。
3. SSL証明書は Cloudflare によって**自動的かつ無料**で発行・適用されますので、追加の設定は不要です。

---

## 2. お問い合わせメールアドレスの変更手順 (GAS)

現在、お問い合わせフォームの送信先（管理者用通知メール）は仮のメールアドレスや開発用のGmailアドレスになっています。これをクライアントの公式メールアドレス（例: `info@really-nailschool.com`）に変更する手順です。

### 【ステップ 1】 Apps Script エディタを開く
1. 問い合わせデータが蓄積されている **「Google スプレッドシート」** を開きます。
2. 上部メニューから **「拡張機能」** ＞ **「Apps Script」** をクリックしてスクリプトエディタを起動します。

---

### 【ステップ 2】 コード内のメールアドレスを書き換える
1. エディタ内のコード（`コード.gs`）から、**通知先メールアドレスの定義箇所**（49行目付近）を探します：
   ```javascript
   // 【変更前】
   const adminEmail = 'YOUR_EMAIL@example.com';
   ```
2. この部分を、クライアントがメールを受信したい公式アドレスに書き換えます：
   ```javascript
   // 【変更後】
   const adminEmail = 'info@really-nailschool.com'; // クライアントの受信メールアドレス
   ```
3. エディタ上部の **「保存（フロッピーディスクのマーク）」** ボタンをクリックして保存します。

---

### 【ステップ 3】 ⚠️ 超重要：新しいバージョンでデプロイを更新する
**ここを忘れると、メールアドレスの変更が本番フォームに反映されません。**

1. 画面右上の青い **「デプロイ」** ボタンをクリックし、**「デプロイを管理」** を選択します。
2. 表示されたデプロイのリストの右上にある **「編集（鉛筆のマーク）」** をクリックします。
3. **「バージョン」** のプルダウンメニューをクリックし、必ず **「新バージョン」** を選択します。
   > **※注意**: バージョン表記が「バージョン 2」「バージョン 3」のようにカウントアップされます。
4. 画面右下の **「デプロイ」** ボタンをクリックします。
5. ウェブアプリのURLが発行されます。変更されていないか確認し、URLをコピーします。
   *(※通常、既存のデプロイを編集してバージョン更新した場合はURLは変わりませんが、念のためコピーしておきます)*

---

### 【ステップ 4】 Webサイト環境変数の更新
もしウェブアプリのURLが新しく変わった場合、あるいは本番ドメイン切り替えに伴い、Cloudflare Pages 側の環境変数を更新します。

1. [Cloudflare ダッシュボード](https://dash.cloudflare.com/) ＞ **「Workers & Pages」** ＞ 本プロジェクト（`really-nailschool`）を開きます。
2. **「設定 (Settings)」** ＞ **「環境変数 (Environment variables)」** をクリックします。
3. `VITE_GAS_URL` の値を、ステップ3でコピーした新しいウェブアプリURLに書き換えて保存します。
4. 変更を有効にするため、Pagesのダッシュボードから **「デプロイを再トリガー（再ビルド）」** してください。

---

## 3. 動作確認チェックリスト（納品前確認）

設定完了後、以下のテストを必ず行ってください。

* [ ] 独自ドメイン（例: `https://really-nailschool.com`）にアクセスし、鍵マーク（SSL保護）が付いた状態でサイトが表示されること。
* [ ] スマートフォンおよびパソコンの両方から表示が崩れていないこと。
* [ ] お問い合わせフォームからテストデータを入力し、送信が「送信中...」を経て完了画面になること。
* [ ] 紐付けられた Google スプレッドシートの末尾に、テスト送信したデータが自動で追記されること。
* [ ] 設定したクライアントの公式メールアドレス宛てに、件名 `【無料資料請求・お問い合わせ】〇〇様より` で自動通知メールが即時届くこと。
