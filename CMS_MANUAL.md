# 🌸 Bloom Nail Academy / Re'ally nail school
# ホームページかんたん更新・管理マニュアル

このマニュアルは、サロンのスタッフ様やスクール運営者様など、**「パソコンやホームページの専門知識があまりない方」**でも迷わずにホームページの文字や画像を更新できるように作られた取扱説明書です。

アメブロやSNS（Instagram）を投稿するような感覚で、誰でもかんたんにホームページの内容を書き換えることができます！

---

## 📅 これだけ読めばOK！かんたん更新3ステップ

ホームページの更新は、いつでも次の**3つの手順**で行うことができます。

### 【ステップ 1】 管理画面（microCMS）にログインする
1. インターネットで **[microCMS（マイクロシーエムエス）ログイン画面](https://microcms.io/)** を開きます。
2. 登録されている「メールアドレス」と「パスワード」を入力してログインします。
3. サービス一覧から **「really-nailschool」** をクリックして開きます。

---

### 【ステップ 2】 文字や画像を書き換える
1. 画面左側のメニューから、変更したい項目を選びます（詳しくは後述の [メニューの見方](#-管理画面メニューの分かりやすい見方) をご覧ください）。
2. 書き換えたい場所の文字を入力し直したり、新しい画像をアップロードします。

---

### 【ステップ 3】 「公開」ボタンを押して、3分待つ
1. 編集が終わったら、画面右上にある赤い **「公開」** ボタンを必ずクリックします。
2. **約2〜3分** 待つと、自動的にインターネット上のホームページに反映されます！
   *(※反映されない場合は、スマホやパソコンのブラウザでページを「再読み込み（リロード）」してください。)*

---

## 📋 管理画面メニューの分かりやすい見方

管理画面の左メニューには、大きく分けて**3つの編集エリア**があります。

```
📂 really-nailschool（管理画面）
 ┣ 📄 サイト全体の設定 (site-settings)   <-- 住所、キャンペーン、スケジュールなどほぼ全て
 ┣ 📂 コース一覧 (courses)              <-- 各コースの料金表や授業内容
 ┗ 📂 受講生の声 (student-voices)        <-- 卒業生・在校生の写真や感想文
```

---

### 📄 1. 「サイト全体の設定 (site-settings)」で変更できること
ここはホームページの「看板」から「足元（フッター）」まで、大部分の設定を行う場所です。

#### 💡 目的別の変更手順

| やりたいこと | 管理画面での変更場所 | 入力のアドバイス |
| :--- | :--- | :--- |
| **一番上のメイン画像を変えたい** | `heroImages` (スライド画像一覧（一番上）) | 画像をドラッグ＆ドロップで複数載せると、自動でスライドショーになります。 |
| **ヒーローエリアのバッジ、タイトル、説明文を変えたい** | `heroBadge` (ヒーローエリア上部バッジ)<br>`heroTitle` (メインタイトル)<br>`heroDesc` (メイン説明文) | ホームページの最上部メインビジュアル内のテキスト群を変更できます。 |
| **ヒーローエリアの2つのボタンの文字とリンク先を変えたい** | `ctaPrimaryText` (右側CTAボタン文字)<br>`ctaPrimaryHref` (右側CTAボタンリンク先)<br>`ctaSecondaryText` (左側CTAボタン文字)<br>`ctaSecondaryHref` (左側CTAボタンリンク先) | 右側（プライマリ）と左側（セカンダリ）のCTAボタンの挙動を設定できます。 |
| **「選ばれる理由」の文字や画像を変えたい** | `reason1Title` / `reason2Title` / `reason3Title` (選ばれる理由1/2/3：タイトル)<br>`reason1Desc` / `reason2Desc` / `reason3Desc` (選ばれる理由1/2/3：説明文)<br>`reason1Image` / `reason2Image` / `reason3Image` (選ばれる理由1/2/3：画像) | スクールが選ばれる理由を3つまで編集できます。説明文やイメージ画像を変更可能です。 |
| **「スクールの特徴」の文字や画像を変えたい** | `feature1Title` / `feature2Title` / `feature3Title` (特徴1/2/3：タイトル)<br>`feature1DescLine1` / `feature1DescLine2` (特徴1：説明文1/2行目)<br>`feature2DescLine1` / `feature2DescLine2` (特徴2：説明文1/2行目)<br>`feature3DescLine1` / `feature3DescLine2` (特徴3：説明文1/2行目)<br>`feature1Image` / `feature2Image` / `feature3Image` (特徴1/2/3：画像) | スクールならではの特徴を3つまで編集できます。説明文はそれぞれ1行目と2行目があります。改行を行いたい場合は `\n` を使用してください。 |
| **「サポート体制」の文字や画像を変えたい** | `support1Badge` / `support2Badge` / `support3Badge` (サポート1/2/3：バッジ名)<br>`support1Title` / `support2Title` / `support3Title` (サポート1/2/3：タイトル)<br>`support1Desc` / `support2Desc` / `support3Desc` (サポート1/2/3：説明文)<br>`support1Image` / `support2Image` / `support3Image` (サポート1/2/3：画像) | 就職保証や独立開業支援などのサポート内容を3つまで編集できます。 |
| **お知らせ・トピックスを書き換えたい** | `topics` (お知らせ・トピックス一覧) | 日付や画像、記事のタイトルを設定できます。 |
| **返金保証のバッジや説明文を変えたい** | `refundBadge` (返金保証：バッジ)<br>`refundTitle` (返金保証：タイトル)<br>`refundDesc` (返金保証：説明文) | 受講生への返金保証に関するバッジ名、タイトル、詳しい説明文を編集できます。 |
| **キャンペーン情報を変えたい** | `campaignTitle` (特別キャンペーン：タイトル)<br>`campaignDesc` (特別キャンペーン：説明文)<br>`campaignBadge` (特別キャンペーン：バッジ)<br>`campaignDeadline` (特別キャンペーン：期限日) | キャンペーンの期限（例：「2026年6月30日まで」）などを自由に変更できます。 |
| **画面の一番上に流れるバナー文字を変えたい** | `campBannerText` (最上部バナー文字) | ホームページの最上部に流れる目立つ帯のメッセージです。 |
| **検定の試験日スケジュールを追加・変更したい** | `scheduleList` (検定試験スケジュール一覧) | 「日付」「試験の名前」「現在の状況（受付中 / 準備中 など）」を設定します。 |
| **合格率や実績の数値を変更したい** | `qualificationRate` (検定合格率(数値))<br>`qualificationTitle` (合格率エリア：タイトル)<br>`qualificationDesc` (合格率エリア：説明文)<br>`qualGuaranteeTitle` (合格保証：タイトル)<br>`qualGuaranteeDesc` (合格保証：説明文)<br>`badge1Label` / `badge1Target` / `badge1Suffix` など<br>`badge2Label` / `badge2Target` / `badge2Suffix` など<br>`badge3Label` / `badge3Target` / `badge3Suffix` など | 合格率や実績バッジ（バッジ1〜3のラベル、数値、単位）を編集できます。「98」や「150」などの**半角数字**を入れるだけで、ホームページ上の円グラフや数字が自動で切り替わります。 |
| **よくある質問（FAQ）を追加・修正したい** | `faqList` (よくある質問一覧) | 質問と回答をセットでいくつでも追加できます。 |
| **キッズスクールの紹介文・料金を変えたい** | `kidsIntro` (キッズスクール：紹介文)<br>`kidsCourses` (キッズスクール：コース一覧)<br>`kidsCourse` (キッズスクール：コース（カスタム設定）) | お子様向けコースの対象年齢、曜日時間、お月謝、チラシ画像、およびコース個別のカスタムパラメータを設定・追加できます。 |
| **SNSアカウントのリンク先を変えたい** | `instagramUrl` (公式InstagramのURL)<br>`twitterUrl` (公式TwitterのURL) | スクール公式の各SNSのリンク先を設定します。 |
| **公式LINEの宛先やQRコード画像を変えたい** | `lineUrl` (公式LINEの宛先URL)<br>`lineQrImage` (公式LINEのQRコード画像) | 新しいQRコード画像を登録すると、お問合せエリアの画像が自動で切り替わります。 |
| **ロゴの文字を変えたい** | `logoText` (スクールロゴ文字) | ヘッダーやフッター等で使用されるスクールロゴの文字テキストを編集します。 |
| **電話番号やスクールの住所を変えたい** | `telNumber` (お問合せ電話番号)<br>`address` (スクールの住所) | ここを書き換えるだけで、ホームページ内の全ての住所や電話番号が一度に書き換わります。 |
| **「選ばれる理由（グリッド）」の内容を書き換えたい** | `reasonsGridList` (選ばれる6つの理由(グリッド)) | ホームページの中段にある「選ばれる6つの理由」のカード群です。理由の番号、タイトル、説明文を設定・追加できます。 |
| **「あなたにぴったりのコース診断」の選択肢を書き換えたい** | `courseDiagnosisList` (おすすめコース診断一覧) | ホームページの中段にあるコース診断の選択肢です。タイプ名（例：TYPE A）、対象、説明文、ボタンの文字、リンク先、ボタンの色を設定できます。 |
| **校舎の名前や駅からのアクセス経路を変えたい** | `campusName` (校舎名)<br>`campusAccess` (最寄り駅からのアクセス経路) | 校舎の名前（例：「京都伏見本校」）や、駅から校舎までのルートテキストを変更できます。改行はそのまま反映されます。 |
| **各種お申し込みボタンの上に表示される小さな文言（マイクロコピー）を変えたい** | `ctaMicroCopy` (体験会CTAマイクロコピー)<br>`ctaRequestMicroCopy` (資料請求CTAマイクロコピー)<br>`ctaTrialMicroCopy` (体験会ボタンマイクロコピー) | ボタンの上の「＼スマホで簡単30秒！／」「＼1分で完了！／」「手ぶらで参加OK♪」などの目立たせる一言文言を変更できます。 |
| **フッター（画面最下部）の紹介文を変えたい** | `footerIntro` (フッター紹介文) | 画面左下のロゴの下に表示されるスクール紹介メッセージを変更できます。改行はそのまま反映されます。 |

---

### 📂 2. 「コース一覧 (courses)」で変更できること
「ライトコース」「実践コース」など、スクールが提供しているレッスンの案内を編集します。

1. メニューから「コース一覧」を開き、編集したいコース名をクリックします。（新しくコースを増やすときは、右上の「追加」ボタンを押します）
2. **主な入力項目**:
   - **コースID**: アルファベットで名前を付けます（例：`course-abc`）。
   - **タイトル / 説明**: コースの名前と、特徴を説明する文章です。
   - **コース画像**: レッスンの様子などの画像を登録します。
   - **おすすめ強調表示 (`isAccent`)**:  
     ここにチェックを入れて「ON」にすると、ホームページ上でそのコースが**ゴールドの枠線で囲まれて目立つように表示**されます！
   - **料金内訳 (`pricing`)**:  
     「入学金」「授業料」などの項目名と、金額を入力します。「重要マークにチェック」を入れると、その項目の説明文が赤色で目立つようになります。

---

### 📂 3. 「受講生の声 (student-voices)」で変更できること
ホームページの真ん中あたりにある、受講生・卒業生のスライドショーを編集します。

1. メニューから「受講生の声」を開き、追加または修正したい人のカードを開きます。
2. **主な入力項目**:
   - **お名前**: 「田中さん（24歳）」などのようにお名前と年齢を入力します。
   - **受講コース**: 「プロフェッショナルコース修了」などと入力します。
   - **見出し**: 「未経験から憧れのサロンへ！」など、一番目立たせたい一言を入力します。
   - **感想本文**: 嬉しかったことやスクールの感想文を入力します。
   - **プロフィール画像**: 受講生の方のお顔写真や、練習中の手元の写真などをアップロードします。

---

## 🎨 知っておくと便利な「文字の飾り・改行」のコツ

### ① 文字を改行したいとき
スマートフォンの画面で見やすくするために、文章の途中で改行したいときは、改行したい位置に `\n` と入力してください。

> **（例）**  
> `直営サロン連携による\n現場直結カリキュラム`  
> 　↓ ホームページ上ではこのように2行で表示されます  
> **直営サロン連携による**  
> **現場直結カリキュラム**

### ② ハートマークや絵文字を使いたいとき
文章の中に `♡` や `✨` などの絵文字をそのまま入力して公開すれば、ホームページ上でも可愛くそのまま表示されます。

---

## 🖼️ 画像を載せるときのやさしい注意点

画像を新しくするときは、以下の点にだけ気をつけてみてください。

1. **写真は「横向き」がおすすめ**  
   メイン画像やコースの画像は、縦長の写真よりも「横長」の写真の方がきれいにフィットします。
2. **スマホで撮った写真はそのまま載せない（推奨）**  
   最近のスマートフォンで撮った写真は画質が良すぎて、ファイルサイズが非常に大きくなっています。そのまま載せると、ホームページを開くのに時間がかかる原因になります。  
   - ネットの無料サービス（例：「[画像圧縮AC](https://www.compressor.ac/)」や「[ILoveIMG](https://www.iloveimg.com/ja/compress-image)」など）を使って、画像の容量を少し小さく（軽量化）してから載せるのがベストです！

---

## 🚨 トラブルシューティング（困ったときは）

### Q1. 「公開」したのにホームページが変わらない！
- **対策1 (少し待つ)**: 変更が反映されるまでに最大で3分ほどかかります。少し待ってから再度確認してください。
- **対策2 (画面の更新)**: ブラウザが古いホームページを記憶して表示している場合があります。
  - **パソコンの場合**: キーボードの `F5` キーを押すか、ブラウザの左上にある「丸い矢印マーク（更新ボタン）」を押してください。
  - **スマートフォンの場合**: 画面を一番上まで引っ張って指を離す（引っ張って更新する）か、ブラウザを一度閉じて開き直してください。

### Q2. 途中で間違えておかしな内容で公開してしまった！
- **対策**: microCMSには「履歴`機能があります。画面右側にある「変更履歴」から、過去の状態を選んで元の正しい状態に戻すことができます。焦らずに過去のバージョンを選んで再度「公開」ボタンを押してください。

---

## 🌐 独自ドメイン（固有ドメイン）に変更したいとき

ホームページのURLを `fastidious-cannoli-79ebcd.netlify.app` から、サロン独自のドメイン（例: `your-school.com`）に変更する方法です。

### ① ホームページ自体（Netlify）を独自ドメインにする手順（推奨）
1. **ドメインの取得**:
   お名前.comやムームードメインなどの外部ドメインサービスで、希望のドメインを購入しておきます。
2. **Netlifyでの追加設定**:
   Netlify管理画面に入り、**「Site configuration」** ＞ **「Domain management」** ＞ **「Add custom domain」** をクリックして取得したドメインを追加します。
3. **ネームサーバーの切り替え**:
   ドメイン追加後、Netlify画面に表示される **4つのネームサーバー情報**（例: `dns1.p01.nsone.net` など）をコピーし、ドメイン購入先（お名前.com等）の管理画面でネームサーバー設定に登録します。
4. **確認**:
   DNS設定が反映されると（数分〜最大24時間）、自動的にHTTPS（常時SSL）で独自ドメインからアクセスできるようになります。

### ② microCMSの画像・API配信を独自ドメインにする手順
アップロードした画像の配信URL（`images.microcms-assets.io`）を独自ドメイン（`images.your-school.com`）にしたい場合：
1. microCMS管理画面の **「サービス設定」** ＞ **「カスタムドメイン」** で配信用のサブドメインを申請します。
2. 指定された CNAME レコードをドメイン管理会社の管理画面に登録します。

---

## ⚙️ ※これより先は「ホームページ制作会社・エンジニア」専用の技術エリアです
サロンのスタッフ様は、ここから先の作業を行う必要はありません。

### ローカル開発環境での動作・確認手順（開発者用）

### 1. 環境変数のセットアップ
プロジェクトのルートに `.env` ファイルを作成し、microCMSの資格情報を定義します。
```env
MICROCMS_SERVICE_DOMAIN=【microCMSサービスドメイン】
MICROCMS_API_KEY=【microCMSのAPIキー】
```

### 2. npmスクリプトの一覧
- **開発サーバー起動 (Vite)**
  ```bash
  npm run dev
  ```
- **microCMSからの最新データ手動同期取得**
  ```bash
  npm run build:cms
  ```
  *(※環境変数がない場合は、fetchSiteContent.js 内の最新 `fallbackContent` が自動で書き出され、ローカル表示を安全に維持します。)*
- **本番ビルド（データ同期 ➔ TSチェック ➔ Viteコンパイル）**
  ```bash
  npm run build
  ```
- **自動テストの実行 (Vitest)**
  ```bash
  npm run test
  ```

---
📅 **マニュアル最終改訂**: 2026年6月5日  
✍️ **制作サポート**: Antigravity Web開発チーム
